import express from 'express';

//import {  } from '../controllers/stockCards.js'
import auth from '../middleware/auth.js'

const router = express.Router();

// router.patch('/stockCards', auth, func)

export default router;